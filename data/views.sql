-- 2022
CREATE OR REPLACE VIEW `2022_february` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2022-02-01' AND '2022-02-28';

CREATE OR REPLACE VIEW `2022_march` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2022-03-01' AND '2022-03-31';

CREATE OR REPLACE VIEW `2022_april` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2022-04-01' AND '2022-04-30';

CREATE OR REPLACE VIEW `2022_may` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2022-05-01' AND '2022-05-31';

CREATE OR REPLACE VIEW `2022_june` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2022-06-01' AND '2022-06-30';

CREATE OR REPLACE VIEW `2022_july` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2022-07-01' AND '2022-07-31';

CREATE OR REPLACE VIEW `2022_august` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2022-08-01' AND '2022-08-31';

CREATE OR REPLACE VIEW `2022_september` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2022-09-01' AND '2022-09-30';

CREATE OR REPLACE VIEW `2022_october` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2022-10-01' AND '2022-10-31';

CREATE OR REPLACE VIEW `2022_november` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2022-11-01' AND '2022-11-30';

CREATE OR REPLACE VIEW `2022_december` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2022-12-01' AND '2022-12-31';

-- 2023
CREATE OR REPLACE VIEW `2023_january` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2023-01-01' AND '2023-01-31';

CREATE OR REPLACE VIEW `2023_february` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2023-02-01' AND '2023-02-28';

CREATE OR REPLACE VIEW `2023_march` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2023-03-01' AND '2023-03-31';

CREATE OR REPLACE VIEW `2023_april` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2023-04-01' AND '2023-04-30';

CREATE OR REPLACE VIEW `2023_may` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2023-05-01' AND '2023-05-31';

CREATE OR REPLACE VIEW `2023_june` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2023-06-01' AND '2023-06-30';

CREATE OR REPLACE VIEW `2023_july` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2023-07-01' AND '2023-07-31';

CREATE OR REPLACE VIEW `2023_august` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2023-08-01' AND '2023-08-31';

CREATE OR REPLACE VIEW `2023_september` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2023-09-01' AND '2023-09-30';

CREATE OR REPLACE VIEW `2023_october` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2023-10-01' AND '2023-10-31';

CREATE OR REPLACE VIEW `2023_november` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2023-11-01' AND '2023-11-30';

CREATE OR REPLACE VIEW `2023_december` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2023-12-01' AND '2023-12-31';

-- 2024
CREATE OR REPLACE VIEW `2024_january` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2024-01-01' AND '2024-01-31';

CREATE OR REPLACE VIEW `2024_february` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2024-02-01' AND '2024-02-29';

CREATE OR REPLACE VIEW `2024_march` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2024-03-01' AND '2024-03-31';

CREATE OR REPLACE VIEW `2024_april` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2024-04-01' AND '2024-04-30';

CREATE OR REPLACE VIEW `2024_may` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2024-05-01' AND '2024-05-31';

CREATE OR REPLACE VIEW `2024_june` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2024-06-01' AND '2024-06-30';

CREATE OR REPLACE VIEW `2024_july` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2024-07-01' AND '2024-07-31';

CREATE OR REPLACE VIEW `2024_august` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2024-08-01' AND '2024-08-31';

CREATE OR REPLACE VIEW `2024_september` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2024-09-01' AND '2024-09-30';

CREATE OR REPLACE VIEW `2024_october` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2024-10-01' AND '2024-10-31';

CREATE OR REPLACE VIEW `2024_november` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2024-11-01' AND '2024-11-30';

CREATE OR REPLACE VIEW `2024_december` AS
SELECT `date`, breakfast, after_breakfast, lunch, after_lunch, dinner, after_dinner, extra, comment
FROM blood_sugar_level
WHERE date BETWEEN '2024-12-01' AND '2024-12-31';